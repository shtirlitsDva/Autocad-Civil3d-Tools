<Window x:Class="DimensioneringV2.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mapsui="clr-namespace:Mapsui.UI.Wpf;assembly=Mapsui.UI.Wpf"
        Title="AutoCAD Map Window" Height="600" Width="800" Foreground="{DynamicResource Text}" Background="{DynamicResource Background}">
    <Window.Resources>
        <SolidColorBrush Color="#3b4453" x:Key="Background"></SolidColorBrush>
        <SolidColorBrush Color="White" x:Key="Text"></SolidColorBrush>
    </Window.Resources>
    <DockPanel>
        <!-- Load Data Button -->
        <Button x:Name="LoadButton" Content="Load Data" Width="100" Height="30" DockPanel.Dock="Top" Margin="10" Click="LoadButton_Click"/>

        <!-- Map Control -->
        <mapsui:MapControl x:Name="MapControl" IsManipulationEnabled="False" Margin="10" DockPanel.Dock="Top"/>

        <!-- Settings Tab Control -->
        <TabControl DockPanel.Dock="Bottom" Margin="10" Background="{DynamicResource Background}" Foreground="{DynamicResource Text}">
            <TabItem Header="Settings">
                <StackPanel Margin="10">
                    <RadioButton Content="Option 1" Margin="5"/>
                    <RadioButton Content="Option 2" Margin="5"/>
                    <CheckBox Content="Enable Feature X" Margin="5"/>
                    <CheckBox Content="Enable Feature Y" Margin="5"/>
                    <Label Content="Double Value" Margin="5"/>
                    <TextBox Width="100" Margin="5"/>
                    <Label Content="Integer Value" Margin="5"/>
                    <TextBox Width="100" Margin="5"/>
                    <Label Content="Select Option" Margin="5"/>
                    <ComboBox Width="100" Margin="5">
                        <ComboBox.ItemsSource>
                            <x:Array Type="{x:Type sys:String}" xmlns:sys="clr-namespace:System;assembly=mscorlib">
                                <sys:String>Option A</sys:String>
                                <sys:String>Option B</sys:String>
                                <sys:String>Option C</sys:String>
                            </x:Array>
                        </ComboBox.ItemsSource>
                    </ComboBox>
                </StackPanel>
            </TabItem>
        </TabControl>
    </DockPanel>
</Window>
