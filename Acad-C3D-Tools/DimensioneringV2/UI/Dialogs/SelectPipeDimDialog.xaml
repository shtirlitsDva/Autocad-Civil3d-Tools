<Window x:Class="DimensioneringV2.UI.Dialogs.SelectPipeDimDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select dimension for pipes" Height="180" Width="360"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Margin="0,0,8,8" VerticalAlignment="Center">Pipe type:</TextBlock>
        <ComboBox Grid.Row="0" Grid.Column="1" Width="220" Margin="0,0,0,8"
                  ItemsSource="{Binding PipeTypes}"
                  SelectedItem="{Binding SelectedPipeType}"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,0,8,8" VerticalAlignment="Center">Pipe dim:</TextBlock>
        <ComboBox Grid.Row="1" Grid.Column="1" Width="220" Margin="0,0,0,8"
                  ItemsSource="{Binding PipeDims}"
                  SelectedItem="{Binding SelectedDim}"/>

        <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Width="80" Margin="0,10,10,0" Content="OK" Command="{Binding OkCommand}"/>
            <Button Width="80" Margin="0,10,0,0" Content="Cancel" Command="{Binding CancelCommand}"/>
        </StackPanel>
    </Grid>
</Window>


