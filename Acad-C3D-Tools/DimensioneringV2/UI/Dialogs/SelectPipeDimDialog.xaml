<Window x:Class="DimensioneringV2.UI.Dialogs.SelectPipeDimDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Pipe Dimension" Height="180" Width="360" WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Margin="0,0,8,8" VerticalAlignment="Center">Type</TextBlock>
        <ComboBox Grid.Row="0" Grid.Column="1" Margin="0,0,0,8"
                  ItemsSource="{Binding PipeTypes}"
                  SelectedItem="{Binding SelectedPipeType, Mode=TwoWay}"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,0,8,8" VerticalAlignment="Center">Nominal</TextBlock>
        <ComboBox Grid.Row="1" Grid.Column="1" Margin="0,0,0,8"
                  ItemsSource="{Binding NominalDiameters}"
                  SelectedItem="{Binding SelectedNominal, Mode=TwoWay}"/>

        <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="OK" Width="70" Margin="0,0,8,0" IsDefault="True" Command="{Binding OkCommand}"/>
            <Button Content="Retry" Width="70" Margin="0,0,8,0" Command="{Binding RetryCommand}"/>
            <Button Content="Cancel" Width="70" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>


