<Window x:Class="NorsynDistrictZones.UI.PriceEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="District Zones — Price Editor"
        Height="620" Width="800"
        WindowStartupLocation="CenterScreen"
        Language="da-DK"
        Background="{DynamicResource Background}">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/NorsynDistrictZones;component/UI/Theme.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Catalog:" VerticalAlignment="Center" Margin="0,0,8,0"
                       Foreground="{DynamicResource Text}" />
            <ComboBox Width="230" ItemsSource="{Binding Catalogs}" DisplayMemberPath="Name"
                      SelectedItem="{Binding SelectedCatalog}"
                      Style="{StaticResource ModernDarkComboBox}" />
        </StackPanel>

        <WrapPanel Grid.Row="1" Margin="0,0,0,10">
            <Button Content="New" Command="{Binding NewCatalogCommand}" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,6,0" />
            <Button Content="Copy" Command="{Binding CopyCatalogCommand}" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,6,0" />
            <Button Content="Rename…" Command="{Binding RenameCatalogCommand}" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,6,0" />
            <Button Content="Delete" Command="{Binding DeleteCatalogCommand}" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,6,0" />
            <Button Content="Set Active" Command="{Binding MakeActiveCommand}" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,6,0" />
            <Button Content="Import…" Command="{Binding ImportCommand}" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,6,0" />
            <Button Content="Export…" Command="{Binding ExportCommand}" Style="{StaticResource SecondaryButtonStyle}" />
        </WrapPanel>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Groups}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Expander Header="{Binding Header}" IsExpanded="False" Margin="0,0,0,6">
                            <DataGrid ItemsSource="{Binding Entries}"
                                      Style="{StaticResource DarkDataGridStyle}"
                                      ColumnHeaderStyle="{StaticResource CompactDataGridColumnHeaderStyle}"
                                      HeadersVisibility="Column"
                                      IsReadOnly="{Binding DataContext.SelectedCatalogIsReadOnly, RelativeSource={RelativeSource AncestorType=Window}}"
                                      AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False"
                                      ScrollViewer.VerticalScrollBarVisibility="Disabled">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Dimension" Binding="{Binding Dn}" IsReadOnly="True" Width="90" />
                                    <DataGridTextColumn Header="DKK / m" Binding="{Binding PricePerMeter, StringFormat=N0, ConverterCulture=da-DK}" Width="*" />
                                    <DataGridTextColumn Header="DKK / pr. stik" Binding="{Binding PricePerFitting, StringFormat=N0, ConverterCulture=da-DK}" Width="*" />
                                </DataGrid.Columns>
                            </DataGrid>
                        </Expander>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <DockPanel Grid.Row="3" Margin="0,10,0,0">
            <TextBlock DockPanel.Dock="Left" VerticalAlignment="Center" Foreground="{DynamicResource Text}">
                <Run Text="Active catalog: " />
                <Run Text="{Binding ActiveName}" FontWeight="Bold" />
            </TextBlock>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="OK" Command="{Binding OkCommand}" Style="{StaticResource PrimaryButtonStyle}" Width="96" Margin="0,0,8,0" />
                <Button Content="Cancel" Command="{Binding CancelCommand}" Style="{StaticResource SecondaryButtonStyle}" Width="96" />
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
